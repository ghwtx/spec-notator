.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Excluded for now — the React prototype and the working spec live outside
# the committed app source.
/prototype-reference
/SPEC.md
